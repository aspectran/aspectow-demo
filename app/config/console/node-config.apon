# Cluster-wide settings
cluster: {
    id: cluster1
    mode: gateway  # direct or gateway
    scheduler: {
        releasedOnUnlock: false
    }
}
group: {
    id: dev-aspectow-demo-group1
    title: Dev Aspectow Demo
    description: This group is for localhost development.
}
